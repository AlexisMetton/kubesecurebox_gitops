apiVersion: v1
kind: Service
metadata:
  name: mcp-hub
  namespace: mcp-hub
  labels:
    app: mcp-hub
spec:
  selector:
    app: mcp-hub
  ports:
    - port: 8000
      targetPort: 8000
      name: http
