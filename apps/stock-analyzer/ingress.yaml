# Ingress Tailscale — https://stocks.kubesecurebox.com
# DNS Cloudflare : A record stocks → IP Tailscale Pi master (proxy désactivé)
# ssl-redirect: false évite ERR_TOO_MANY_REDIRECTS (Tailscale termine le TLS côté client).
# Ordre des paths : /api/auth avant /api (auth Next.js), puis API FastAPI, puis UI.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-analyzer-ingress
  namespace: stock-analyzer
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/upstream-vhost: "stocks.kubesecurebox.com"
spec:
  ingressClassName: nginx
  rules:
    - host: stocks.kubesecurebox.com
      http:
        paths:
          - path: /api/auth
            pathType: Prefix
            backend:
              service:
                name: stock-analyzer-frontend
                port:
                  number: 3000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: stock-analyzer-backend
                port:
                  number: 8000
          - path: /docs
            pathType: Prefix
            backend:
              service:
                name: stock-analyzer-backend
                port:
                  number: 8000
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: stock-analyzer-backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stock-analyzer-frontend
                port:
                  number: 3000
  tls:
    - hosts:
        - stocks.kubesecurebox.com
      secretName: stock-analyzer-kubesecurebox-tls
