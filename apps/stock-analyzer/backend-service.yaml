apiVersion: v1
kind: Service
metadata:
  name: stock-analyzer-backend
  namespace: stock-analyzer
  labels:
    app: stock-analyzer-backend
spec:
  selector:
    app: stock-analyzer-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
