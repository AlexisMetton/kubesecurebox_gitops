# Ingress n8n - HTTPS via cert-manager
# =====================================
# Public via Cloudflare Tunnel: le tunnel envoie du HTTP a l'ingress.
# ssl-redirect: false evite les boucles de redirection HTTPS cote client.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n-ingress
  namespace: n8n
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/upstream-vhost: "n8n.kubesecurebox.com"
spec:
  ingressClassName: nginx
  rules:
    - host: n8n.kubesecurebox.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n8n
                port:
                  number: 5678
  tls:
    - hosts:
        - n8n.kubesecurebox.com
      secretName: n8n-kubesecurebox-tls
